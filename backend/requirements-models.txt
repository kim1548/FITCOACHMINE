# 운동 영상 분석 사이드카(video_service.py / pose_pipeline.py) 전용 의존성.
# Python 3.10 권장 (mediapipe 0.10.14 + scikit-learn 1.3.2가 3.10 휠 제공).
#
# 버전 고정이 중요한 것:
#   - scikit-learn==1.3.2 : 모델(.pkl)이 1.3.0 학습본 → 1.3.x 아니면 예측이 깨짐
#   - numpy==1.26.4       : sklearn 1.3.2가 numpy<2 요구 (2.x면 binary 충돌)
#   - mediapipe==0.10.14  : 클래식 solutions API(mp.solutions.pose) 필요 (최신 빌드엔 빠져 있음)
#
# 설치 (각자 PC에서):
#   py -3.10 -m venv .venv310
#   .venv310\Scripts\python.exe -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   .venv310\Scripts\python.exe -m pip install -r backend\requirements-models.txt
# 실행:
#   powershell -ExecutionPolicy Bypass -File backend\run_dev.ps1
#
# 참고: YOLOv5는 torch.hub(ultralytics/yolov5:v7.0)로 최초 1회 인터넷에서 받아 캐시됨.

# --- 핵심 (버전 고정 필수) ---
numpy==1.26.4
scikit-learn==1.3.2
mediapipe==0.10.14

# --- 영상/포즈 처리 ---
opencv-python
pandas
pillow
imageio
imageio-ffmpeg

# --- YOLOv5(torch.hub) 런타임 의존성 ---
ipython
pyyaml
scipy
tqdm
matplotlib
gitpython
thop
seaborn
requests
psutil

# --- 서비스 ---
fastapi
uvicorn
pydantic
httpx

# torch / torchvision 은 위 CPU 인덱스로 별도 설치 (여기 두면 GPU판이 잡힐 수 있음)
